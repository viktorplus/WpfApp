<UserControl x:Class="WpfApp.Pages.ScheduleList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Cursor="">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView x:Name="ScheduleListView" ItemsSource="{Binding Schedules, RelativeSource={RelativeSource AncestorType={x:Type local:ScheduleList}}}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Subject" DisplayMemberBinding="{Binding Subject.Name}" />
                    <GridViewColumn Header="Group" DisplayMemberBinding="{Binding Group.GroupName}" />
                    <GridViewColumn Header="Classroom" DisplayMemberBinding="{Binding Classroom.RoomNumber}" />
                    <GridViewColumn Header="Building" DisplayMemberBinding="{Binding Building.BuildingNumber}" />
                    <GridViewColumn Header="Date" DisplayMemberBinding="{Binding Date}" />
                    <GridViewColumn Header="Time" DisplayMemberBinding="{Binding Time}" />
                    <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding Lecturer.FirstName}" />
                    <GridViewColumn Header="Фамилия" DisplayMemberBinding="{Binding Lecturer.LastName}" />

                </GridView>
            </ListView.View>
        </ListView>




        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Grid.Row="3" Cursor="">
            <Button Content="Prev 10" Click="PreviousPageButton_Click"  Margin="5" />
            <Button Content="Next 10" Click="NextPageButton_Click"  Margin="5" />

        </StackPanel>
        <!-- Кнопка для отображения формы добавления предмета --><!--
        <Button Content="Add Schedule" Click="ShowAddSubjectFormButton_Click" Grid.Row="0" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center" />
        --><!-- Форма для добавления предмета (изначально скрыта) --><!--
        <Grid x:Name="AddSubjectForm" Grid.Row="1" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                <TextBox x:Name="SubjectNameTextBox" Width="150" Margin="5" HorizontalAlignment="Center" VerticalAlignment="Center" />
                <Button Content="Add to List" Click="AddToSubjectListButton_Click" Margin="5" HorizontalAlignment="Left" VerticalAlignment="Center" />
            </StackPanel>
        </Grid>-->
    </Grid>
</UserControl>
