 ﻿<UserControl x:Class="WpfApp.Pages.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <Label Content="Логин:" Grid.Row="0" Grid.Column="1"/>
        <TextBox Name="LoginTextBox" Grid.Row="0" Grid.Column="2" Width="200"/>

        <Label Content="Пароль:" Grid.Row="1" Grid.Column="1"/>
        <PasswordBox Name="PasswordBox" Grid.Row="1" Grid.Column="2" Width="200"/>

        <Button Content="Войти" Grid.Row="2" Grid.Column="2" Margin="50" Width="100" Click="LoginButton_Click" Background="{DynamicResource MaterialDesignDarkBackground}"/>
    </Grid>
</UserControl>