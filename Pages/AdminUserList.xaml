<UserControl x:Class="WpfApp.AdminUserList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView x:Name="userListView" Margin="10" ItemsSource="{Binding Users}">
            <ListView.View>
                <GridView>
                    <!-- Добавляем колонки для свойств пользователя -->
                    <GridViewColumn Header="Username" DisplayMemberBinding="{Binding Username}" Width="120"/>
                    <!-- Добавьте еще колонки по необходимости -->
                </GridView>
            </ListView.View>
        </ListView>
        <Button Content="Загрузить следующие 50 пользователей" Click="LoadNextUsers_Click" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10" />
    </Grid>
</UserControl>
