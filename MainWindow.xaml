<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        Title="MainWindow" Height="450" Width="800" KeyDown="tbText_KeyDown" KeyUp="Window_KeyUp">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Margin="10 10 10 10" x:Name="tbText" Text="hello world"
                 Height="150" VerticalAlignment="Top" materialDesign:HintAssist.Hint="This is a limited text area"
                 
                 AcceptsReturn="True" IsEnabled="{Binding Path=IsChecked, ElementName=MaterialDesignOutlinedTextBoxEnabledComboBox}"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" TextChanged="TextBox_TextChanged"/>
        <StackPanel Grid.Row="1" Grid.ColumnSpan="3"  x:Name="SPBase4" Orientation="Vertical"/>
        <StackPanel Grid.Row="2" Grid.ColumnSpan="3"  Margin="25 0 0 0" x:Name="SPBase" Orientation="Vertical"/>
        <StackPanel Grid.Row="3" Grid.ColumnSpan="3"  Margin="50 0 0 0" x:Name="SPBase2" Orientation="Vertical"/>
        <StackPanel Grid.Row="4" Grid.ColumnSpan="3"  Margin="75 0 0 0" x:Name="SPBase3" Orientation="Vertical"/>
        <materialDesign:ColorZone Grid.Row="5" Grid.ColumnSpan="3"  Margin="0 0 0 0" 
              materialDesign:ElevationAssist.Elevation="Dp4"
              ClipToBounds="False" CornerRadius="10" Mode="PrimaryDark">
            <StackPanel Orientation="Horizontal">
                <ToggleButton
                            Style="{StaticResource MaterialDesignHamburgerToggleButton}" />
                <TextBlock
                Margin="16,0,0,0" 
                x:Name="statsTextBlock"
                VerticalAlignment="Center" />
            </StackPanel>
        </materialDesign:ColorZone>
    </Grid>
</Window>

