<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="500" Background="Black" Foreground="White">
    <Window.Resources>
    </Window.Resources>


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="ErrorMessageTextBlock" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="4" VerticalAlignment="Center" Foreground="Red"  Margin="50 0"/>

        <StackPanel Grid.Column="1" Grid.Row="1">
            <TextBlock Text="Логин:" Margin="0 5 0 0"/>
            <TextBlock Text="Пароль:" Margin="0 25 0 0"/>

        </StackPanel>
        <StackPanel Grid.Column="2" Grid.Row="1">
            <TextBox Name="UsernameTextBox" Margin="0 0 0 5"/>
            <PasswordBox Name="PasswordBox" Margin="0 10 0 5"/>
            <CheckBox Content="Запомнить пользователя" Margin="0 10 0 0" Foreground="White"/>
        </StackPanel>
        <StackPanel Grid.Column="2" Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Stretch">
            <Button Content="Login" Style="{StaticResource GreenButtonStyle}" Click="Login_Click" />
            <Button Content="Cancel" Style="{StaticResource RedButtonStyle}" Click="Cancel_Click" />
        </StackPanel>


    </Grid>
</Window>
